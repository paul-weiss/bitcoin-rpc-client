# Regtest mode
regtest=1

# RPC settings
server=1
rpcuser=your_username
rpcpassword=your_password
rpcport=18443
rpcallowip=127.0.0.1

# Optional: Set the data directory to keep regtest data in the project directory
datadir=./regtest-data

# Optional: Enable wallet
wallet=1

# Optional: Set logging
debug=1
logtimestamps=1 